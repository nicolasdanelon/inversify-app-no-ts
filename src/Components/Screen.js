import React from 'react';

const Screen = ({ children }) => (
    <div className="App">
        <header className="App-header">
            {children}
        </header>
    </div>
);

export default Screen;
